version: '2'
services:
  nextcloud:
    image: wonderfall/nextcloud
    links:
      - nextcloud-db:nextcloud-db   # If using MySQL
      - solr:solr                   # If using Nextant
      - redis:redis                 # If using Redis
    environment:
      - UID=1000
      - GID=1000
      - UPLOAD_MAX_SIZE=10G
      - APC_SHM_SIZE=128M
      - OPCACHE_MEM_SIZE=128
      - CRON_PERIOD=15m
      - TZ=Europe/Berlin
      - ADMIN_USER=admin            # Don't set to configure through browser
      - ADMIN_PASSWORD=admin        # Don't set to configure through browser
      - DOMAIN=laurt.com
      - DB_TYPE=mysql
      - DB_NAME=nextcloud
      - DB_USER=nextcloud
      - DB_PASSWORD=..maoqiang
      - DB_HOST=nextcloud-db
    volumes:
      - /Users/z/works/docker/nextcloud/data:/data
      - /Users/z/works/docker/nextcloud/config:/config
      - /Users/z/works/docker/nextcloud/apps:/apps2
      - /Users/z/works/docker/nextcloud/themes:/nextcloud/themes
    ports:
      - "9999:9999"


  # If using MySQL
  nextcloud-db:
    image: mariadb:10
    volumes:
      - /Users/z/works/docker/nextcloud/db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=..maoqiang
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=nextcloud
      - MYSQL_PASSWORD=..maoqiang


  # If using Nextant
  solr:
    image: solr:6-alpine
    container_name: solr
    volumes:
      - /Users/z/works/docker/nextcloud/solr:/opt/solr/server/solr/mycores
    entrypoint:
      - docker-entrypoint.sh
      - solr-precreate
      - nextant

      
  # If using Redis
  redis:
    image: redis:alpine
    container_name: redis
    volumes:
      - /Users/z/works/docker/nextcloud/redis:/data
